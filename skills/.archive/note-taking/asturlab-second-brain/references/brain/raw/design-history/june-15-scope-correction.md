# June 15 Scope Correction — Second Brain vs War Room

## Trigger

Use this reference when Rawan asks whether War Room belongs inside the active second-brain build, or when a later session appears to have merged `brain`, `Dreaming`, and `War Room OS` into one implementation track.

## Source correction

Rawan pointed back to the June 15 22:06 brief and corrected Qayid's interpretation.

That June 15 brief was a second-brain build brief, not a War Room build brief.

It asked to improve the AsturLAB vault / second-brain system using paired Claude Code ultracode and the Karpathy LLM Wiki pattern, with supporting research from OpenHuman, gbrain, GrowthClaw, and two YouTube sources.

## Scope of the June 15 brief

Build the AsturLAB second-brain system:

- raw sources preserved untouched
- `wiki/` contains only `index.md`, `log.md`, `processed.md`
- all topic/entity/synthesis/source-summary pages live in `raw/pages/`
- ingest operation
- query operation
- Dream Sequence / lint health check
- index + log discipline
- session capture
- E-compress before synthesis
- processed ledger for NEW detection
- eventual scheduled Dream / maintenance after manual protocols prove themselves
- day-to-day usage: `add this`, `save this session`, `what do I know about ___`, `save that`, `run dream sequence`

## What went wrong

A later kickoff included “brain/Dreaming/War Room OS foundation” and “move War Room earlier.” Qayid treated that as permission to implement War Room slices inside the second-brain window.

That was scope bleed.

Correct interpretation:

1. Read/check War Room access only if requested by preflight.
2. Mark War Room as a separate future track.
3. Complete the second-brain core first.
4. Prove manual brain operations: ingest, query, session capture, index/log/processed, and Dream.
5. Only then run strategy session / War Room OS, unless Rawan explicitly re-approves changing the active build order.

## Durable rule

Do not let a later mixed phrase override the original task source. If Rawan references a prior timestamp/message, retrieve the exact message and use it to govern scope.

War Room can move earlier than qmd, cron, tier-2 auto-pages, and other advanced brain automation. It does not move into Phase 1 of the second-brain build by default.

## Build-quality implication

Moving War Room fully to the end can reduce operating-context grounding if the brain is built in a vacuum.

But moving War Room into the beginning can overload the foundation and pollute scope.

Correct compromise:

- During brain build: allow inert War Room awareness and access checks only.
- After brain core/routing/lint pass: run the strategy session.
- Then build War Room OS as its own test-gated system.

## Completion test for “second brain created”

The second brain is not created just because the scaffold exists or War Room templates exist.

It is created when these pass manually:

- query works
- ingest works
- session capture works
- index/log/processed works
- manual Dream works
- routing does not hijack normal vault saves
- no qmd/cron/client/War Room coupling is required for Phase 1
